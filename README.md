# Azure Linux Droidspaces Rootfs

This repository builds and publishes an **AArch64 Azure Linux 3.0 userspace rootfs tarball**
intended for use with **Droidspaces**.

The published artifact is a gzip-compressed root filesystem archive:

```text
azurelinux-<resolved-version>-aarch64-droidspaces-rootfs.tar.gz
